<h1>Students</h1>


<ul>
  <% @students.each do |s| %>

  <li><%=  "Name: " %> <%= link_to s.to_s, student_path(s) %></li>
    <% end %>
  </ul>

    <%= link_to "Create new student", new_student_path %>
