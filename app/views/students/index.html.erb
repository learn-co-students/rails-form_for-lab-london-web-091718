<h1>These Are All The Students</h1>
<%= link_to "Create New Student", new_student_url %>
<% @students.each do |student| %>
<p><%= link_to student.first_name + ' ' + student.last_name, student_url(student) %></p>
<% end %>
